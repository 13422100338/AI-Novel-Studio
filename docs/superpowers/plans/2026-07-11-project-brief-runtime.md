# 真实项目 Chapter Brief 接入

## 目标

让 Brief 对话框读写当前项目数据库，并通过既有来源指纹与生命周期服务完成保存、冻结和克隆；冻结后解锁 Standard/Strict 正文生成。

## 最小改动

1. 新增项目级 Brief 门面，组装既有仓库、上下文编译器与生命周期服务。
2. Brief 对话框增加项目数据绑定和保存/冻结/克隆信号，同时保留无项目时的演示行为。
3. MainWindow 负责协调 UI 与项目门面，不让控件直接访问数据库。
4. 用 UI 集成测试验证“打开 → 编辑 → 冻结 → 解锁 Standard”。
